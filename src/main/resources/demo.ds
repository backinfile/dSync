
root struct DBoard {
	DHuman[] humans;
	EBoardState state;
}

struct DHuman {
	long id;
	string name;
	DCardPile handPile; // card
	String[] cards;
}

struct DCardPile {
	DCard[] cards;
}

struct DCard {
	long id;
}

enum EBoardState {
	Normal; // normal
	Run; // run
}