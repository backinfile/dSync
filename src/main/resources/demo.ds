
root struct DBoard {
	DHuman[] humans;
}

// 玩家信息
struct DHuman {
	long id;
	string name;
	DCardPile handPile; // 手牌
	String[] cards;
}

struct DCardPile {
	DCard[] cards;
}

struct DCard {
	long id;
}