
root struct DBoard {
	DHuman[] humans;
	EBoardState state;
}

// comment test1
// comment test2
struct DHuman {
	long[] id;
	float percent;
	float[] percents;
	string name;
	DCardPile handPile; // field comment
	String[] cards;
}

struct DCardPile {
	DCard[] cards;
}

struct DCard {
	long id;
}

enum EBoardState {
	Normal; // normal
	Run; // run
}